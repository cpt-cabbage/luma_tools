# Luma Tools Requirements
# Python 3.10+ required
# Install in virtual environment: pip install -r requirements.txt

# Core Qt GUI - Upgraded to PySide6 for NumPy 2.x compatibility and macOS support
# PySide6_Addons includes QtWebEngineWidgets for Three.js 3D viewer (WebGL via QWebEngineView)
PySide6>=6.6.0
QDarkStyle==3.2.3
QtPy==2.4.2  # QDarkStyle dependency

# File sequence handling
fileseq==2.1.2

# Utilities
six>=1.16.0
typing_extensions>=4.12.2
packaging>=24.1

# Windows-specific
pywin32==308

# Numerical
numpy>=1.26.4

# Image processing
pillow>=12.1.0

# =============================================================================
# 3D Model Support
# =============================================================================

# Primary loader - bundles Assimp, supports FBX, GLB, OBJ, DAE, STL, PLY, etc.
open3d>=0.18.0

# Fallback loader for GLB/GLTF/OBJ (native support, no external dependencies)
trimesh>=4.10.1
networkx>=3.4.2  # trimesh dependency
freetype-py>=2.5.1  # trimesh dependency

# OpenGL rendering for 3D viewer
PyOpenGL>=3.1.0
PyOpenGL-accelerate>=3.1.10

# USD support (for USD/USDA/USDC/USDZ files)
usd-core>=25.11

# =============================================================================
# Optional Features
# =============================================================================

# Spell checking for text prompts
pyenchant>=3.3.0

# AYON integration (optional - for publishing to AYON)
ayon-python-api>=1.2.4
